# Ticker

Second val — trying the cron trigger.

- `cron.ts` — logs a timestamp on each run.

Unlike HTTP vals, the schedule isn't set in code or via `vt`: after
pushing, open the val on val.town and add a CRON trigger via the `+`
button in the editor (or the triple-dots menu next to Run), then pick an
interval or a cron expression.
